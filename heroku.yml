build:
  docker:
    backend: ruby/Dockerfile
    frontend: frontend/Dockerfile
  config:
    REACT_APP_API_HOST=http://ruby:8000
# run:
#   backend: ruby ruby/app.rb -o 0.0.0.0
#   frontend: cd frontend && npm start
